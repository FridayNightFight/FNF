private _numberOfObjectives = 10;
private _numberOfTransportsPerSide = 3;


/*
Usage:

Quick-configure:
Use the pre-placed scavenger objectives and transports. Delete any you don't wish to use and update the numbers above.

Custom objects:
1. Delete the pre-placed scavenger objectives or transports.
2. Create the objects you wish to use, and assign them a variable name called
	"scav_obj_#"
	or
	"scav_transport_#"
where # is a number between 1 and the number configured above

NOTE: You MUST use side-appropriate transport vehicles that match the participating sides.
*/
